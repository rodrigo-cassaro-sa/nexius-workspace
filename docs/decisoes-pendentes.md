# Decisões Pendentes

Registro central de informações que faltam ou decisões em aberto identificadas durante a validação da documentação (`docs/validacao/00-validacao-documentacao.md`).

Status: aberto = aguardando definição; resolvido = decidido e refletido na descrição do produto.

Convenção: D = decisão. Bloqueante = impede parte do MVP.

---

## Bloqueiam o MVP — RESOLVIDAS

Todas refletidas em `01-descricao-produto.md` (§9, §16 e §19).

| ID | Tema | Decisão | Impacto | Status |
|---|---|---|---|---|
| D1 | Convites | Validade 7 dias, uso único; reenvio gera novo token e invalida o anterior; perfil já definido no convite. | Cadastro e Administração | resolvido |
| D2 | Recuperação de senha | Token com validade de 30 minutos, uso único (entidade `tokens_recuperacao`). | Recuperação de senha | resolvido |
| D3 | Escopo do Colaborador | Envolvido = responsável de ao menos uma ação da demanda OU autor de comentário em alguma ação dela. | Permissões e consultas | resolvido |
| D4 | Escopo do Gestor | Gestor vê todas as demandas; só o Colaborador é restrito. Sem entidade de equipe. | Permissões e consultas | resolvido |
| D5 | Observadores de ação | Responsável da ação + criador da demanda + autores de comentário na ação. Derivado em runtime (sem tabela no MVP). | Notificações | resolvido |
| D6 | Status enumerados | Demanda: aberta, em_andamento, concluida, arquivada, cancelada. Ação: pendente, bloqueada, concluida, cancelada. | Banco e validação | resolvido |

---

## Não bloqueiam o início — RESOLVIDAS

| ID | Tema | Decisão | Status |
|---|---|---|---|
| D7 | Fila de e-mail | Tabela `fila_email` + cron, com reprocessamento limitado. | resolvido |
| D8 | Provedor de e-mail | SMTP configurado no backend (credenciais protegidas). | resolvido |
| D9 | Onboarding | Flag de onboarding concluído como campo em `usuarios`. | resolvido |
| D10 | Pré-requisito | Múltiplos por ação — tabela `acao_prerequisitos`; sem dependência circular. | resolvido |
| D11 | Configurações | Sem tela separada no MVP; ajustes (dados, senha, tema) no Perfil. | resolvido |
| D12 | Suporte | E-mail de suporte (mailto) na tela de Ajuda. | resolvido |
| D13 | Não funcionais / visual | Disponibilidade: melhor esforço (SLA na infra). Ícones: CDN externo. Modo escuro: incluído no MVP. | resolvido |

---

## Itens de implementação — RESOLVIDOS

- **Bootstrap do primeiro administrador**: resolvido. **Primeiro cadastro vira admin**, de forma controlada: enquanto a tabela `usuarios` estiver vazia, uma tela/endpoint de setup permite criar a primeira conta, que recebe perfil `administrador`. Assim que existir ao menos um usuário, esse caminho é desativado e o acesso volta a ser exclusivamente por convite. ✔
- **CDN/conjunto de ícones**: **Lucide** via CDN. ✔

## Ainda em aberto (não bloqueiam documentação nem modelagem)

- **D14 — Gamificação: incluída no escopo (decisão de produto).** Originalmente fora do MVP em todos os documentos; por decisão do dono do produto, a **v1** entra no escopo: progresso **pessoal, sem ranking**, com pontos **derivados das ações reais** (sem tabela nova/sem migration), níveis, conquistas e tela `progresso.html`. Regras em `docs/gamificacao/10-plano-gamificacao.md`. Implementado: `includes/gamificacao.php`, `api/gamificacao/progresso.php`, `public/progresso.html`, `public/js/gamificacao.js`, item "Progresso" no menu. Descrição §17/§23 atualizadas. Doc refletida também em `03-mapa-de-telas.md` e `03-modelagem-banco-dados.md` (§10) na revisão geral de documentação. Status: **resolvido (implementado)**.
- **D15 — Resumo periódico por e-mail (digest) / push (retenção).** Os e-mails do MVP são **operacionais por evento** (`01-descricao-produto.md` §16); não há **digest/resumo periódico** nem **push** definidos. A retenção do MVP usa Dashboard (pendências, "minhas ações", "continue de onde parou"), notificações por evento e histórico (ver `docs/retencao/11-plano-retencao-usuario.md`). Para adicionar digest/push é preciso **decisão de produto** + regras de **consentimento/opt-out e anti-spam** (`boas-praticas-notificacoes.md`). Status: **IMPLEMENTADO (digest por e-mail)** — **Migration 014** (`usuarios.digest_ativo` + `digest_enviado_em`); `includes/digest.php` + `cron/enviar-digest.php` (monta o resumo pessoal reaproveitando o Dashboard e **enfileira na `fila_email`**; idempotente/anti-spam: não reenvia dentro de ~6 dias e não envia se não houver pendências/atrasadas); **opt-out** no Perfil (toggle + `api/perfil/preferencias.php`). **Pendências operacionais:** o cron `enviar-digest.php` (semanal) e o `processar-fila-email.php` (a cada 5 min) **são agendados automaticamente pela imagem** (Dockerfile + `docker/app-cron`); a entrega ainda **depende do domínio de e-mail verificado na Resend**. **Push: FORA do escopo** — decisão do dono do produto. O badge ao vivo (polling) + os e-mails cobrem a retenção; push exigiria Service Worker/VAPID/consentimento (complexidade alta para um sistema interno). Não será feito. Ver `docs/desenvolvimento/15-plano-evolucoes.md` (item 1).
- **D16 — Tela "Ações" (lista global de ações) + visão de calendário.** Não constava no inventário de telas (mapa §731), mas consolida elementos já documentados (lista de ações; filtros como na lista de demandas; popup de detalhe da ação; escopo de visibilidade) e corresponde ao slot "Plano de ação" do mockup. Incluída por **decisão de produto**; refletida no `03-mapa-de-telas.md`. Implementado: `public/acoes.html`, `public/js/acoes.js`, `api/acoes/listar-todas.php`, funções `montar_where_acoes`/`listar_acoes` em `includes/acoes.php`, item "Ações" no menu. A mesma tela ganhou um **alternador Lista/Calendário** (decisão de produto): o calendário posiciona as ações pelo **prazo** numa grade mensal, reaproveitando escopo e filtros, sem tabela nova (`api/acoes/calendario.php` + `listar_acoes_calendario`, intervalo de datas limitado a ~6 semanas). Só ações com prazo aparecem no calendário. **Sem tabela nova** (usa `acoes`). Status: **resolvido (implementado)**.
- **D17 — Anexos na criação de demandas (uploads).** Originalmente **fora do MVP** em todos os documentos (`01-arquitetura-mvp.md` §117/§290 e `03-modelagem-banco-dados.md` §159 listam "uploads" como não modelado/não construir). Por **decisão de produto** (dono do produto), anexar arquivos ao criar uma demanda entra no escopo. Implementação **mínima e segura**, seguindo `boas-praticas-seguranca.md` §9 e `boas-praticas-backend.md` §21: tabela nova `anexos` (só metadados); arquivos salvos em **pasta privada fora da raiz pública** (`storage/anexos/`, com `.htaccess` de bloqueio e `.gitignore`); validação no backend de **tamanho** (10 MB), **extensão (allowlist)** e **MIME real (finfo)**; arquivo **renomeado** (nome aleatório, nunca o original); **sem execução** do arquivo; download apenas via `api/anexos/baixar.php` com **login + escopo** e `Content-Disposition: attachment` + `nosniff`. Endpoints: `api/anexos/enviar.php` (POST multipart, Gestor/Admin), `listar.php` (GET, escopo), `baixar.php` (GET, escopo). Front: campo de arquivos no modal "Nova demanda" + lista de anexos (download) no detalhe da demanda. Config: `ANEXOS_DIR`, `ANEXO_TAMANHO_MAX`, `ANEXOS_MAX_POR_ENVIO`. **Migration 008** (`008_add_anexos.sql`) precisa ser rodada em produção; o `ANEXOS_DIR` deve apontar para um **volume persistente** no EasyPanel. Status: **resolvido (implementado)**.
- **D18 — Anexos em comentários de ação.** Por **decisão de produto**, ao comentar em uma ação é possível anexar arquivos. Reaproveita **a mesma tabela `anexos`**, **a mesma pasta privada** (`storage/anexos/`) e **a mesma validação** dos anexos de demanda (`boas-praticas-seguranca.md` §9 / `boas-praticas-backend.md` §21). Sem tabela nova: **Migration 009** (`009_add_anexo_comentario.sql`) adiciona a coluna `comentario_id` (NULL = anexo da demanda; preenchido = anexo do comentário; `demanda_id` continua gravado para o escopo). Permissão: **só o autor do comentário** anexa nele (`api/anexos/enviar-comentario.php`); a listagem (`api/anexos/listar-comentarios.php`) e o download (`api/anexos/baixar.php`) respeitam o escopo de quem vê a demanda. A lógica de upload foi extraída para o helper `processar_anexos_upload()` em `includes/anexos.php`, usado tanto por demanda quanto por comentário. Front: input de arquivo no formulário de comentário + lista de anexos por comentário em `demanda.html`/`demanda.js`. **Migration 009 precisa ser rodada no ambiente** (mesma observação da 008 sobre o volume persistente de `storage/anexos`). Status: **resolvido (implementado)**.
- **D19 — Tipos de tarefa (ação) + recusa de entrega.** Por **decisão de produto**, cada ação passa a ter um **tipo**: `analise`, `desenvolvimento`, `entrega`, `incidente` (ajudam a entender/planejar/executar a demanda). Não constava em nenhum documento (status de ação só era `pendente/bloqueada/concluida/cancelada`, ver D6); entra como nova decisão. Regras: **análise** só conclui com **arquivo anexado** (evidência); **desenvolvimento** conclui normal; **entrega** conclui normal, mas pode ser **recusada** — exigindo um **motivo** (novo status `recusada` + campo `motivo_recusa`); **incidente** é registro/relato (conclui normal). **Sem tabela nova:** `acoes` ganha `tipo` e `motivo_recusa` e o enum de status ganha `recusada` (**Migration 010**); a evidência da análise reaproveita a tabela/pasta de anexos (`anexos.acao_id`). Permissões: criar com tipo = Gestor/Admin; concluir = responsável (análise exige anexo); enviar evidência = responsável ou Gestor/Admin; **recusar entrega = Gestor/Admin** (decisão adotada por falta de definição — o responsável entrega, quem gerencia revisa/recusa). Endpoints: `api/acoes/recusar.php`, `api/anexos/enviar-acao.php`, `api/anexos/listar-acoes.php` (download pelo mesmo `baixar.php`). Front: seletor de tipo na nova ação, campo de arquivo na conclusão de análise, botão "Recusar" + modal de motivo, badge de tipo e exibição do motivo/evidências no plano de ação. **Migration 010 precisa ser rodada no ambiente.** ~~Limitação registrada: uma ação **chave** recusada deixa a demanda sem conclusão automática (não há fluxo de reabertura no MVP).~~ **Limitação resolvida na melhoria #4** (`docs/desenvolvimento/16-plano-melhorias.md`): há botão **"Reabrir"** (Gestor/Admin) que devolve a ação recusada para `pendente` e limpa o `motivo_recusa` — `reabrir_acao()` + `api/acoes/reabrir.php`. Status: **resolvido (implementado)**.

  **Ampliação (Migration 012) — tipo `reuniao`:** novo tipo de tarefa, decisão de produto. Conclui apenas com **um anexo (a ata da reunião)** — reaproveita o mesmo mecanismo de evidência do tipo `analise` (`anexos.acao_id`), generalizado em `acao_tipo_exige_anexo()`. Pode ter **várias pessoas envolvidas**: tabela nova `acao_participantes` (relação ação↔usuários). Os participantes (a) **contam como "envolvidos"** para o escopo de visibilidade do colaborador — atualizado em `colaborador_envolvido_na_demanda` (demandas.php) e `montar_where_acoes` (acoes.php) — e (b) recebem **notificação** ao serem incluídos. Endpoint novo só de leitura: `api/acoes/participantes.php` (escopo da demanda). Front: opção "Reunião" no tipo, seletor múltiplo de participantes (visível só para reunião), campo da ata na conclusão (rótulo dinâmico), e exibição de "Envolvidos" na ação. Conclusão segue por conta do **responsável** (participantes não concluem). **Migration 012 rodada no ambiente.** **Evoluções decididas (ver `docs/desenvolvimento/15-plano-evolucoes.md`):** (a) **participantes editáveis** após a criação da reunião — **IMPLEMENTADO** (`api/acoes/participantes-definir.php` + modal "Gerenciar participantes"; Gestor/Admin ou responsável; sem tabela nova); (b) **registrar as decisões/regras tomadas na reunião e exibi-las na demanda** — **IMPLEMENTADO** (**Migration 013** `acoes.decisoes`; texto obrigatório ao concluir a reunião, junto da ata; seção "Decisões das reuniões" no detalhe da demanda com link para a ata).
- **D20 — Chat/mensageria entre usuários (solicitado, escopo em aberto).** Pedido: conversa usuário-a-usuário com anexos, links, data de envio/visualização, notificação de nova mensagem, busca de mensagens, exportação da conversa e referência a tarefas/demandas. **Não consta em nenhum documento** (briefing, descrição, mapa de telas, arquitetura, banco). O problema "conversas que ninguém recupera" (briefing) é resolvido no MVP por **comentários por ação + histórico + notificações**, não por chat. Conflitos: regra de "não criar tela/tabela sem evidência" e "não implementar fora do MVP"; o pedido agrega ~8 sub-recursos de uma vez; a stack (sem WebSocket) implica *polling*. Parte já existe em comentários (discussão recuperável, anexos D18, timestamps, vínculo a ação/demanda, notificação de comentário). **Net-new:** espaço de conversa próprio entre pessoas, busca de mensagens, exportação, recibo de leitura por mensagem, notificação "nova mensagem". **Decisão de escopo necessária antes de codar:** (A) reforçar os comentários existentes (menor, alinhado ao MVP); (B) chat 1:1 mínimo, em fases (Fase 1: enviar/listar mensagens + data de envio/leitura + notificação + link para demanda/ação; Fases seguintes: anexos, busca, exportação); (C) chat completo como descrito (grande, multi-fase). **Decisão do dono do produto: opção (B) — chat 1:1 mínimo, em fases.**

  **Fase 1 — IMPLEMENTADA (Migration 011):** chat 1:1 entre usuários — tabelas novas `conversas` (par canônico de usuários) e `mensagens` (texto, `criado_em` = data de envio, `lida_em` = data de visualização marcada quando o outro abre, `demanda_id` = referência opcional). Endpoints `api/chat/`: `conversas-listar.php`, `abrir.php`, `mensagens-listar.php`, `enviar.php`, `nao-lidas.php`. Tela `chat.html` + `js/chat.js` (lista de conversas + thread + envio, *polling* leve de ~12s); item **Mensagens** com **badge de não lidas** na sidebar de todas as telas internas (atualizado em `ui.js`). "Notificação de nova mensagem" = **contador de não lidas** (anti-spam, sem 1 notificação por mensagem). Links em mensagens viram clicáveis com segurança (sem `innerHTML`). Permissões: qualquer usuário logado conversa com outro **ativo**; só vê conversas das quais participa; referência a demanda exige que o **remetente** possa vê-la (escopo). **Migration 011 precisa ser rodada no ambiente.**

  **Fases seguintes:** **CANCELADAS por decisão do dono do produto** — o chat **não vai evoluir** (anexos por mensagem, busca, exportação e referência a ação não serão feitos). A **Fase 1 permanece** como está. Ver `docs/desenvolvimento/15-plano-evolucoes.md` (item 2). Status: **Fase 1 mantida; sem novas fases.**
- **D21 — Setores.** Por **decisão de produto**, cada usuário pertence a um **setor** (lista fixa: Comercial, Relacionamento, Logística, Roteirização, Equipe Externa, Fechamento, Financeiro e — Migration 017 — Diretoria Financeira (CFO), Diretoria Comercial (CCO), Diretoria Operacional (COO), Diretoria Presidência (CEO), RH, Tecnologia). O conceito é citado no briefing ("organizar demandas entre **setores** e pessoas"), mas não era modelado. Regras: a **demanda herda o setor do criador** (automático na criação); ao criar uma **ação**, o responsável vem **pré-selecionado** com o **responsável principal do setor** da demanda (editável — decisão do dono). **Migration 015:** tabela nova `setores` (`id`, `nome`, `responsavel_id`) + **seed dos 7**; colunas `usuarios.setor_id` e `demandas.setor_id` (**sem coluna nova em `acoes`** — a ação herda o setor pela demanda). Admin define o setor de cada usuário e o responsável principal de cada setor na tela **Usuários** (card "Setores"). Endpoints: `api/setores/listar.php`, `api/setores/definir-responsavel.php`, `api/usuarios/definir-setor.php`. O setor é exibido no detalhe da demanda. **Key user:** o "responsável principal do setor" **é o key user** do setor (testa/analisa/resolve as tarefas do setor) — além de ser o responsável sugerido nas ações, ele **enxerga todas as demandas e ações do seu setor** (escopo do colaborador estendido em `montar_where_demandas`, `colaborador_envolvido_na_demanda`, `montar_where_acoes` e em `contar_demandas_por_status`). **Sem tabela/conceito novo** — reaproveita `setores.responsavel_id` (não foi criada entidade "keyuser" separada, para não duplicar). **Ampliação (melhoria #5):** além de enxergar, o key user pode **concluir as tarefas do seu setor** (não só as próprias) — `usuario_eh_keyuser_da_demanda()` libera `api/acoes/concluir.php` e `api/anexos/enviar-acao.php`; a assinatura/lastro é mantida e o responsável da ação é notificado quando o key user conclui no lugar dele. Também é possível **definir o setor já no convite** (**Migration 016**: `convites.setor_id` + seletor no formulário de convite); ao aceitar, o usuário já entra com esse setor. Permissões: gerir setor de usuário e responsável de setor = **Administrador**. **Migrations 015, 016 e 017 precisam ser rodadas no ambiente.** Status: **resolvido (implementado)**.
- **D22 — Projeto agrupando demandas (melhoria #3).** Por **decisão do dono do produto**, a empresa **trabalha por projetos que agrupam várias demandas**; entra o conceito de **Projeto**. Decisões de modelagem confirmadas: **status espelha a demanda** (`aberto`, `em_andamento`, `concluido`, `arquivado`, `cancelado`); o projeto tem **responsável e setor próprios, ambos opcionais**; **visibilidade por envolvimento** (Gestor/Admin veem todos; Colaborador vê os projetos em que é responsável, key user do setor do projeto, ou que tenham ao menos uma demanda em que esteja envolvido — mesma regra das demandas). **Migration 018:** tabela nova `projetos` (`nome`, `descricao`, `status`, `responsavel_id`, `setor_id`, `criador_id`) + coluna `demandas.projeto_id` (FK opcional, **`ON DELETE SET NULL`** — apagar projeto não apaga demanda). `includes/projetos.php` (CRUD + escopo); endpoints `api/projetos/` (`listar`, `criar`, `detalhe`, `atualizar`, `arquivar`). Vínculo demanda↔projeto: select no **modal de nova demanda** e controle **"Mover para projeto"** no detalhe da demanda (`api/demandas/definir-projeto.php`, Gestor/Admin); o detalhe da demanda mostra o projeto (link). Telas novas `projetos.html` (lista + criar) e `projeto.html` (detalhe + editar/arquivar + demandas vinculadas, reaproveitando `api/demandas/listar.php?projeto=ID`); item **Projetos** no menu de todas as telas internas. Permissões: criar/editar/arquivar projeto e mover demanda = **Gestor/Admin**; ver = escopo. **Migration 018 precisa ser rodada no ambiente.** Status: **resolvido (implementado)**.
- **Logs/auditoria — persistência no banco + retenção de 1 ano.** Por **pedido do dono do produto**, todas as ações relevantes devem ficar **registradas em log**, com **duração de 1 ano**. Antes, `registrar_log()` gravava **apenas em arquivo** (`logs/app.log`), que no container EasyPanel é **efêmero** (some no redeploy). Agora `registrar_log()` **persiste na tabela `logs`** (fonte oficial: `usuario_id`, `acao`, `ip`, `user_agent`, `detalhes`, `criado_em`), mantendo a cópia em arquivo como best-effort; o log **nunca quebra a operação** (falhas são silenciosas, com guarda de reentrância contra recursão na falha de conexão). Cobertura ampliada: além das ações já logadas (login/logout, demandas, ações — criar/concluir/recusar/reabrir/chave/participantes, projetos, convites, usuários, perfil, setores, comentários, chat, relatórios), agora também os **uploads de anexo** (`anexo_enviado_demanda|comentario|acao`). **Retenção:** `limpar_logs_antigos()` remove registros com mais de 1 ano; rotina `cron/limpar-logs.php` (CLI). **Tela de Auditoria (Admin):** `auditoria.html` + `api/logs/listar.php` e `acoes.php` (`listar_logs()` em `includes/log.php`) — consulta os logs com filtros (usuário, ação, período, busca) e paginação, direto no portal. **Pendências operacionais:** **Migration 019 aplicada ✔** (tabela `logs` garantida; os logs já persistem no banco). O cron de retenção (`cron/limpar-logs.php`, 03:00 diário, horário de Brasília) **é agendado automaticamente pela imagem** (Dockerfile + `docker/app-cron`; o container usa timezone America/Sao_Paulo). **Redeploy feito ✔ — os 3 crons (fila de e-mail, digest, limpar-logs) já estão ativos.** Status: **resolvido (implementado)**.
- **D23 — Roadmap / Gráfico de Gantt (linha do tempo).** Por **pedido do dono do produto**, uma visão de **roadmap** que mostra a fila de **projetos, demandas e tarefas** no tempo (pelo **prazo**), para dar um *overview* a todos (da diretoria ao colaborador), tornar visível a **lacuna de tempo** e o **impacto** de novas entradas, e permitir **prorrogação de prazos**. Não constava no mapa de telas (havia a visão de **Calendário** das Ações, D16, que posiciona ações por dia; o roadmap é uma **linha do tempo contínua/Gantt**, complementar). **Sem tabela nova:** usa `acoes` (`prazo` = fim da barra, `criado_em` = início, `status`), agrupadas por `demandas` e `projetos`. `listar_acoes_roadmap()` em `includes/acoes.php` **reaproveita `montar_where_acoes`** (mesmo escopo/filtros — Colaborador só vê o que está no seu escopo; foi adicionado filtro `projeto` ao `montar_where_acoes`). Endpoints: `api/roadmap/listar.php` (GET, só leitura, janela de datas com default ~3 meses) e `api/acoes/definir-prazo.php` (POST). Tela `roadmap.html` + `roadmap.js` (Gantt em HTML/CSS/JS puro, sem bibliotecas; barras coloridas por situação reusando a lógica do calendário; linha do "hoje"; rolagem horizontal no mobile) + item **Roadmap** no menu. **Editar no roadmap:** clicar numa barra abre o popup para editar **responsável e prazo**; além disso, **arrastar a barra** (mouse) desloca o prazo em dias (rasta e solta). Podem editar: **Gestor/Admin, o key user do setor da demanda ou o próprio responsável da tarefa** (`definir_prazo_acao()` / `definir_responsavel_acao()` via `api/acoes/definir-prazo.php` e `definir-responsavel.php`, com `usuario_eh_keyuser_da_demanda`); não altera tarefa concluída/cancelada; o responsável (novo) é notificado quando outra pessoa altera. Enquanto arrasta, uma **dica flutuante mostra a nova data**. No **corpo da barra** o arraste é só com **mouse** (no toque, mantém tap = popup e rolagem); há uma **alça na ponta direita** da barra que permite arrastar também no **toque** (a alça não rola a timeline). **Barras-resumo:** cada linha de **projeto** e de **demanda** mostra uma barra-resumo (span do início mais cedo ao prazo mais tarde do grupo), dando o overview do roadmap em cada nível. **Visão de carga (agrupar por):** seletor que troca o agrupamento da timeline entre **Projeto/Demanda** (padrão), **Responsável** (fila de tarefas de cada pessoa/key user pelo prazo) e **Setor** — cada grupo mostra a contagem de tarefas e a barra-resumo, para enxergar **pessoa ou setor sobrecarregado**. Sem dado novo (usa o mesmo `listar_acoes_roadmap`, que passou a retornar também `setor_nome`). **Sem migração.** Status: **resolvido (implementado)**.
- **D24 — Impacto de prioridade no cronograma (sinalização).** Por **pedido do dono do produto**, o portal deve deixar claro o **impacto** de uma nova demanda/tarefa no cronograma: uma entrada de **maior prioridade** "empurra" e atrasa as de **menor prioridade**. **Decisões do dono (confirmadas):** (a) o sistema **apenas sinaliza** o impacto — **não recalcula prazos automaticamente** (não altera dados de terceiros); (b) **sem campo novo** — usa o que já existe: **prioridade = GUT da demanda**, `prazo` e `responsavel` da ação. **Regra de sinalização (heurística de visualização, não altera dados):** no Roadmap, uma tarefa fica **"em risco de atraso"** quando **outra tarefa do mesmo responsável, com prioridade GUT estritamente maior, concorre no mesmo período** (as janelas `criado_em → prazo` se sobrepõem); só considera tarefas em aberto. Implementado em `public/js/roadmap.js` (`calcularRisco`), sem endpoint novo — `listar_acoes_roadmap` passou a retornar `prioridade` (GUT) e `setor_nome`. **Fonte única (backend):** a heurística vive em `includes/impacto.php` (`sql_condicao_em_risco()` + `acoes_em_risco_ids()` / `demandas_em_risco_ids()` + wrappers), reaproveitada por: **Roadmap** (o endpoint marca `em_risco` em cada item — o JS não recalcula), **detalhe da demanda** (aviso "esta demanda tem N tarefa(s) em risco" via `contar_acoes_em_risco_da_demanda`), **Dashboard** (card de alerta "N tarefas em risco por prioridade" que só aparece quando há risco — `contar_acoes_em_risco`, com escopo: Colaborador vê as suas, Gestor/Admin o total), **lista de Demandas** (⚠ na linha da demanda com tarefa em risco) e **tela de Ações** (⚠ na linha da lista e no evento do calendário). **UX:** aviso no topo do Roadmap com a contagem; barra com contorno tracejado vermelho + ⚠ no título; cabeçalhos de projeto/demanda e de responsável/setor marcam ⚠ / "N em risco". Vale em todos os agrupamentos (projeto, responsável, setor). **Limitação consciente:** sem campo de esforço/capacidade, o "atraso" não é calculado em dias — é sinalizado como **conflito de prioridade na fila do responsável**; um modelo com esforço+capacidade (recálculo real de datas) fica para uma fase futura, se for decidido. **Sem migração.** Status: **resolvido (implementado)**.
- **D25 — Revisão anti-frágil do controle (lacunas do objetivo "controlar tudo com prazos e responsáveis").** Diagnóstico do dono do produto: verificar pontos cegos no controle. Implementado em 4 frentes: (1) **Painel de Controle/Higiene** (`higiene.html` + `includes/higiene.php` + `api/higiene/resumo.php`, Gestor/Admin): lista o que escapa do controle — demandas sem plano de ação, demandas sem responsável, tarefas sem prazo, **tarefas de usuários inativos (órfãs, com reatribuição inline)** e demandas paradas há +N dias; (2) **Dono da demanda** — `demandas.responsavel_id` agora é definível/exibido (`api/demandas/definir-responsavel.php`), prestação de contas no nível da demanda; (3) **Prazo em Projeto e Demanda** (**Migration 020**: `projetos.prazo` e `demandas.prazo`) — permite controlar prazo no nível certo, inclusive antes de haver plano de ação; (4) **Padrões de falha nos Relatórios** — atrasos por responsável (concluídas fora do prazo no período) e recusas por setor. **Limitação consciente:** a sobrecarga/impacto continua **qualitativa** (heurística D24), pois não há modelo de esforço/capacidade — recálculo real de datas fica para fase futura, se decidido. **Migration 020 aplicada ✔.** Status: **resolvido (implementado)**.
- **Backup do banco** — **IMPLEMENTADO e VALIDADO no container ✔** (cron diário 02:00 → `cron/backup-banco.sh` → `storage/backups/*.sql.gz`, retenção ~12 meses; `default-mysql-client` no Dockerfile). Ajustes necessários: `ssl-verify-server-cert=0` (servidor com cert self-signed na VPC) e `--no-tablespaces` sem `--routines/--triggers/--events` (o usuário `nexius_app` não tem `PROCESS`/`EVENT`; schema só de tabelas). Teste manual gerou dump real (KB, com `CREATE TABLE`). Ver `docs/operacao/18-backup-banco.md`. **Pendências operacionais:** (a) garantir **volume persistente** cobrindo `storage/backups` (senão os backups somem no redeploy); (b) **cópia offsite** — **adiada para a ÚLTIMA ETAPA** por decisão do dono (o backup do container fica no mesmo host do app).
- Endereço do **e-mail de suporte** exibido na tela de Ajuda — definir depois (placeholder em `config` até ter o oficial).
- **Logo definitiva** do Grupo Nexius — **RESOLVIDO.** Arquivos em `public/assets/img/`: `logo_escuro.png` (logo **branca, transparente** — usada na sidebar e no painel de login, que são marrons nos dois temas) e `logo_claro.png` (logo **escura sobre branco** — reservada para superfícies claras/brancas). O texto placeholder foi substituído pela logo branca em todas as telas. PNGs **otimizados** (~73 KB / ~139 KB).
- **Envio real de e-mail — CONFIGURADO (Resend), com pendência de domínio.** O provedor **Resend** está implementado e **as variáveis foram configuradas no ambiente** (`RESEND_API_KEY`, `APP_URL`, `SMTP_REMETENTE`, etc.). A integração funciona, mas hoje usa o remetente de **sandbox** `onboarding@resend.dev`, que a Resend só entrega para o **e-mail dono da conta** (`rodrigo@saimunizacoes.com.br`); para outros destinatários ela retorna **403 validation_error** (registrado na coluna `erro` da `fila_email`). **Pendência (parado por decisão):** ainda **não há o domínio definitivo**; quando houver, é preciso **verificar o domínio na Resend** (DNS SPF/DKIM) e trocar `SMTP_REMETENTE` para um endereço desse domínio — aí o envio passa a funcionar para qualquer destinatário. Sem alteração de código. Enquanto isso, dá para testar mandando só para o e-mail dono da conta.
- **E-mail das notificações de eventos:** as notificações **internas** (atribuição, comentário, status, conclusão) estão implementadas. O **e-mail** dos mesmos eventos é canal do MVP, mas só será enfileirado/enviado quando o SMTP estiver configurado (fase de e-mail). Contrato/template definido em `docs/notificacoes/09-plano-notificacoes.md`. Hoje os eventos geram apenas a notificação interna.

## Inconsistências de documentação — corrigidas

- `01-descricao-produto.md` §24: identidade visual já marcada como resolvida.
- Pendências consolidadas neste arquivo.
- Caminho do mockup: **corrigido** — o arquivo foi movido para `docs/design/mockups/mockup-telas.png` (caminho que o guia e o README já apontavam).
- Briefing "Atribuições/responsáveis": confirmado como campo (responsável em demanda/ação), não tabela separada.
